import React from "react"

export const ReactXStateContext = React.createContext(null)

// if (process.env.NODE_ENV !== "production") {
//   ReactXStateContext.displayName = "ReactXState"
// }

export default ReactXStateContext
